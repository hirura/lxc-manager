function create_user() {
	window.location.href = "/create_user";
}

function edit_user(id) {
	window.location.href = "/edit_user/" + id;
}

function destroy_user(id) {
	window.location.href = "/destroy_user/" + id;
}

function create_host() {
	window.location.href = "/create_host";
}

function edit_host(id) {
	window.location.href = "/edit_host/" + id;
}

function destroy_host(id) {
	window.location.href = "/destroy_host/" + id;
}

function create_distro() {
	window.location.href = "/create_distro";
}

function edit_distro(id) {
	window.location.href = "/edit_distro/" + id;
}

function destroy_distro(id) {
	window.location.href = "/destroy_distro/" + id;
}

function create_network() {
	window.location.href = "/create_network";
}

function edit_network(id) {
	window.location.href = "/edit_network/" + id;
}

function destroy_network(id) {
	window.location.href = "/destroy_network/" + id;
}

function create_container() {
	window.location.href = "/create_container";
}

function destroy_container(id) {
	window.location.href = "/destroy_container/" + id;
}

function start_container(id) {
	window.location.href = "/start_container/" + id;
}

function stop_container(id) {
	window.location.href = "/stop_container/" + id;
}

function container_detail(id) {
	window.location.href = "/container_detail/" + id;
}

function edit_container(id) {
	window.location.href = "/edit_container/" + id;
}

function create_interface(id) {
	window.location.href = "/create_interface/" + id;
}

function edit_interface(id) {
	window.location.href = "/edit_interface/" + id;
}

function destroy_interface(id) {
	window.location.href = "/destroy_interface/" + id;
}

function create_napt(id) {
	window.location.href = "/create_napt/" + id;
}

function edit_napt(id) {
	window.location.href = "/edit_napt/" + id;
}

function destroy_napt(id) {
	window.location.href = "/destroy_napt/" + id;
}

function create_reverse_proxy(id) {
	window.location.href = "/create_reverse_proxy/" + id;
}

function edit_reverse_proxy(id) {
	window.location.href = "/edit_reverse_proxy/" + id;
}

function destroy_reverse_proxy(id) {
	window.location.href = "/destroy_reverse_proxy/" + id;
}

function create_snapshot(id) {
	window.location.href = "/create_snapshot/" + id;
}

function destroy_snapshot(id) {
	window.location.href = "/destroy_snapshot/" + id;
}

function snapshot_detail(id) {
	window.location.href = "/snapshot_detail/" + id;
}

function edit_snapshot(id) {
	window.location.href = "/edit_snapshot/" + id;
}

function create_clone(id) {
	window.location.href = "/create_clone/" + id;
}

function promote(id) {
	window.location.href = "/promote/" + id;
}
